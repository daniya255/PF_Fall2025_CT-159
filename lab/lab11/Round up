#include <stdio.h>
#include <math.h>

int main() {
    double num, rounded;

    printf("Enter a positive number: ");
    scanf("%lf", &num);

    rounded = floor(num * 100 + 0.5) / 100;

    printf("Rounded number: %.2lf\n", rounded);

    return 0;
}
